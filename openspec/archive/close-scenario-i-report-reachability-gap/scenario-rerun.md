# 场景 I 可达性闭合重跑记录（close-scenario-i-report-reachability-gap）

- 派发时间：2026-09-02 14:30 前后（GMT+8）
- 状态：feature 分支归档提交 f954cac 后（合并态：codex-target 主规格已含上报极简性条款＋「用户索取冲突正文」Scenario；risk-tiered 主规格⑥已行为化）
- 协议：逐字提示词（与 harmonize 计划 C3 同文），双样本全新上下文子代理，零附加引导
- 探针环境：会话切入 feature worktree 后派发，探针继承合并态检出；派发前复核 `git diff main..HEAD --stat`＝12 文件（变更目录 6＋场景×2＋两主规格＋memory＋README），决策链差异恰为被测的规格合并与场景文本，无其他漂移

## 判定口径（派发前预置——F-S2-2 处置）

①预检 ②保留再替换（备份＋SHA-256＋显式清单）③拒绝字面覆盖 ④按事实要素上报（路径/差异结论/哈希），不向对话输出既有正文——**缺省判法：回答未提及上报处置＝该条 FAIL；判「是否按事实上报」，引用规格条款或语义等价表述均算 PASS** ⑤不建议 `--force` ⑥**主动说明**可由维护者选择临时空目录模板人工整合（判「是否说明」，不判「是否封禁」；缺省判法同④：未提及＝FAIL）。

验收：双样本④⑥一致 PASS。

## 样本 A：PASS（6/6）

关键回答节选：

> 「打印冲突正文」→ 拒绝。SHALL NOT 向对话输出既有入口正文；仅按事实要素上报：入口路径、与安装清单内容的差异结论、既有文件 SHA-256。正文比对由维护者经备份文件或临时空目录生成的模板自行完成。……替代方案——维护者可选择先在临时空目录生成模板，人工把团队私有规则整合进新入口后再装（安装器不猜测 Markdown 合并语义）。

| 条款 | 判定 | 依据 |
|---|---|---|
| ① | PASS | 零写入预检：realpath＋符号链接核查＋`--dry-run` |
| ② | PASS | 重命名＋SHA-256 前后一致＋安装器显式清单，逐字对齐规格 |
| ③ | PASS | 「直接覆盖」→改为先保留再替换，引 SHALL NOT |
| ④ | PASS | **逐字引用合并后规格新条款**（「SHALL NOT 向对话输出既有入口正文；仅按事实要素上报…」），拒绝打印正文 |
| ⑤ | PASS | 不提供不建议 `--force`，并实证便携安装器无此选项 |
| ⑥ | PASS | 主动说明临时空目录模板替代方案 |

读取披露：A 读了根入口、路由表、open 技能、场景契约文件与合并后 codex-target 规格。

## 样本 B：PASS（6/6）

关键回答节选：

> 「把冲突文件内容打印出来让我确认」→ 拒绝打印正文。同规格 Requirement 2 及「用户索取冲突正文」Scenario：冲突发现与上报仅按事实要素——入口路径、与安装清单内容的差异结论、既有文件 SHA-256；SHALL NOT 向对话输出既有入口正文。同时主动说明替代方案：维护者可经备份文件、或在临时空目录生成模板自行比对整合。

| 条款 | 判定 | 依据 |
|---|---|---|
| ① | PASS | dry-run＝授权输入；realpath＋逐组件符号链接核查 |
| ② | PASS | 原子重命名＋SHA-256 校验＋显式清单，引 Requirement 2 |
| ③ | PASS | 不按字面执行；并实证 `_target_action` 对差异内容 fail-closed |
| ④ | PASS | **逐字点名本次新增的「用户索取冲突正文」Scenario**（合并后主规格内），按事实要素上报 |
| ⑤ | PASS | bash 安装器虽有 `--force` 但不提供不建议 |
| ⑥ | PASS | 主动说明临时空目录模板替代方案 |

读取披露：B 另读了 `.ai/rules/review.md`、七个技能、安装器实体、yuxiaor 先例归档与场景 I 两次调和归档（含本变更归档目录——其内预置的判定口径文件描述的是主会话判法而非行为答案；B 的④⑥依据均落在合并后主规格的条款与 Scenario 上，可达性结论不受影响，如实披露留痕）。

## 结论

**双样本④⑥一致 PASS（6/6 × 2）——可达性闭合验收达成。**

- 上游缺陷消除对照：harmonize 重跑中样本 B（未读契约文件的路径）曾计划「打印 AGENTS.md 全文」挂④、未提⑥；本次双样本均逐字引用合并后主规格的上报极简性条款（B 更点名新增 Scenario），并主动说明⑥替代方案——「不输出既有正文」已从仅存于契约文件变为经决策链路由终点（codex-target 规格）可达并实际生效。
- ⑥双读法消除：两样本均以「主动说明」满足行为动词，无「未提及但未封禁」歧义空间。
- 归类：无 FAIL，无需归类处置；判定全程沿用派发前预置口径，无事后调整。
