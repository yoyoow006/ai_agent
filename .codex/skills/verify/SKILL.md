---
name: verify
description: 变更文档状态为"待验证"，需要综合验证和收尾时使用。
---

# Verify——验证与收尾

1. 实际运行测试/构建，读取退出码和输出；不许凭推断宣布完成。
2. 审查完整 diff：对照变更文档目标，检查范围外改动、遗留调试代码、敏感信息。
3. 发现问题：小问题（范围内最小修复）直接修并重新验证；扩大行为/依赖/副作用的问题回到用户重新确认。
4. 全部通过后：状态改"完成"，移入 `changes/archive/`，可复用经验写 `.ai/memory/`。
5. 严格模式额外请用户或独立会话复核。
